source 'https://rubygems.org'

# Runtime — needed by the Jekyll plugin (codetalk.rb) and by build.rb.
gem 'rouge', '~> 4.0'

# Build-only — used by build.rb to render the standalone demo index.html
# without Jekyll.
gem 'kramdown', '~> 2.4'
gem 'kramdown-parser-gfm', '~> 1.1'
gem 'sass-embedded', '~> 1.0'
